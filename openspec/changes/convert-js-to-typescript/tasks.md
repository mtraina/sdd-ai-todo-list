## Implementation Tasks

- [ ] Add TypeScript support to the frontend by installing `typescript`, `@types/react`, and `@types/react-dom`.
- [ ] Create `frontend/tsconfig.json` with suitable React/JSX settings for Vite.
- [ ] Convert `frontend/src/main.jsx` to `frontend/src/main.tsx` and update imports.
- [ ] Convert `frontend/src/App.jsx` to `frontend/src/App.tsx` with typed props and state.
- [ ] Convert `frontend/src/components/TodoList.jsx` to `frontend/src/components/TodoList.tsx` with typed props.
- [ ] Add a typed `Todo` interface for frontend API models.
- [ ] If needed, rename or convert `vite.config.js` to `vite.config.ts`.
- [ ] Verify `npm install`, `npm run dev`, and `npm run build` in the frontend.
